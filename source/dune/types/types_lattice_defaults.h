#pragma once

/* Struct members on own line. */
/* clang-format off */

/* Lattice Struct */
#define _TYPES_DEFAULT_Lattice \
  { \
    .flag = LT_GRID, \
    .typeu = KEY_BSPLINE, \
    .typev = KEY_BSPLINE, \
    .typew = KEY_BSPLINE, \
    .actbp = LT_ACTBP_NONE, \
  }

/* clang-format on */
